spring:
  config:
    import:
      - optional:file:.env[.properties]
  profiles:
    active: db, s3, swagger

openai:
  model: ${OPENAI_MODEL}
  api:
    key: ${OPENAI_API_KEY}
    image: ${OPENAI_API_KEY_IMAGE}
    url: ${OPENAI_API_URL}
